#===============================================================================
# LYOKO ONLYOFFICE - SECRETS
#===============================================================================
# NOTE: Replace placeholders before applying or use sealed-secrets/external-secrets
apiVersion: v1
kind: Secret
metadata:
  name: onlyoffice-secrets
  namespace: lyoko-onlyoffice
type: Opaque
stringData:
  # JWT secret for document server authentication
  # Generate with: openssl rand -hex 32
  jwt-secret: "${ONLYOFFICE_JWT_SECRET}"
